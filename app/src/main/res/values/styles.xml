<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
          TODO: For this build to work, there must be appcompat and play services as modules. trailapp module depends on
          both of them. Check module SDK for all of them. Also, add libraries for the v4 and v7 support library
    -->

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="windowActionBar">false</item>
        <item name="android:listDivider">@color/myDividerColor</item>
    </style>

</resources>
